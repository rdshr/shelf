# 0.0.48

## 中文

- Shelf AI 已跟随仓库完成重构，当前主链明确为：
  `Framework Markdown -> Package Registry -> Project Config -> Code -> Evidence`。
- 插件里的项目配置跳转已切到 `projects/*/project.toml`，不再把旧双轨配置文件当作主入口。
- 插件侧的自动物化、生成物保护、预检与提示说明都已切到 `project.toml + canonical_graph.json`。
- `projects/*/generated/canonical_graph.json` 是唯一机器真相源；治理树、治理清单和校验报告都按 canonical 派生视图处理。

## English

- Shelf AI now follows the rewritten repository architecture:
  `Framework Markdown -> Package Registry -> Project Config -> Code -> Evidence`.
- Extension-side project navigation now targets `projects/*/project.toml`; the removed dual-track config files are no longer treated as live entrypoints.
- Auto-materialization, generated-artifact guarding, codegen preflight, and packaged guidance now all align with `project.toml + canonical_graph.json`.
- `projects/*/generated/canonical_graph.json` is the only machine truth; governance manifests, governance trees, and reports are treated as canonical-derived views.
